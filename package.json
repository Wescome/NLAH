{
  "name": "nlah",
  "version": "0.1.0",
  "description": "TypeScript-first Natural-Language Agent Harness runtime for artifact-gated coding swarms.",
  "type": "module",
  "bin": {
    "nlah": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "verify": "pnpm run typecheck && pnpm run test"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "execa": "^9.5.2",
    "yaml": "^2.6.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
